pipeline {
  agent any
  stages {
      stage("build"){
        echo 'building th eapplication'
      }
      stage("test"){
        echo 'testing th eapplication'
      }
      stage("deploy"){
        echo 'deploying th eapplication'
      }
  }
}
